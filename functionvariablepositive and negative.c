#include<stdio.h>
int main()
{
    int a,b;
    float sum=0;
    printf("enter the value of a and b");
    scanf("%d %d",&a,&b);
    sum(a,b);
    return 0;
}
  int sum(float x,int y)
    {
        float s;
        s=x+y;
        return(f);
    }

